node_modules/
dist/
*.tsbuildinfo
*.zip

# Credentials — never commit these
.env
.env.docker
.env.local
.env.*.local
.DS_Store

# Smoke-test dumps. Not secret, but a complete record of somebody's holdings
# and every transaction they ever made — `--json` writes one by design, and it
# should not be one `git add -A` away from being published.
*-dataset.json
# Local diagnostic dumps: they contain holdings, activity ids and ledger rows.
.diag-*
full.json

# Workspace build output
**/dist/
