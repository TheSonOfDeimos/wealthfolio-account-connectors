{
  "name": "@wealthfolio-connectors/connector-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Shared pieces every Wealthfolio broker/bank connector needs: sandbox egress, keyring credentials, account linking, and asset-currency repair.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "@wealthfolio/addon-sdk": "^3.6.2"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "verify": "pnpm type-check",
    "build": "echo 'connector-kit ships as TypeScript source; nothing to build.'",
    "clean": "echo 'nothing to clean'"
  }
}